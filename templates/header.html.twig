<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a href="{{ path('app_homepage') }}" class="navbar-brand">{{ 'header.title'|trans }}</a>
        </div>

        <div class="nav navbar-nav navbar-right">
            {% if is_granted('ROLE_USER') %}
                <li>
                    <a href="{{ path('app_books_add') }}">
                        <span class="glyphicon glyphicon-book"></span> Добавить книгу
                    </a>
                </li>

                <li>
                    <a href="{{ path('fos_user_profile_show') }}">
                        <span class="glyphicon glyphicon-user"></span> {{ 'header.profile'|trans }}
                    </a>
                </li>

                <li>
                    <a href="{{ path('fos_user_security_logout') }}">
                        <span class="glyphicon glyphicon-log-out"></span> {{ 'header.logout'|trans }}
                    </a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('fos_user_security_login') }}">
                        <span class="glyphicon glyphicon-log-in"></span> {{ 'header.login'|trans }}
                    </a>
                </li>

                <li>
                    <a href="{{ path('fos_user_registration_register') }}">
                        <span class="glyphicon glyphicon-user"></span> {{ 'header.register'|trans }}
                    </a>
                </li>
            {% endif %}
        </div>
    </div>
</nav>